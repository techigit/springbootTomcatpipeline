spring:
  application:
    name: SpringbootTomcatCICDPipelineAPPlication
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    url: jdbc:mysql://localhost:3306/javatechi?allowPublicKeyRetrieval=true&useSSL=false
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL8Dialect
#  profiles:
#    active:
#    - test
logging:
  file:
    name: C:/Users/Srinu/Desktop/logs/springbootapplication.log
management:
  tracing:
    sampling:
      probability: 1.0